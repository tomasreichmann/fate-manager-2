import {
  AlertContent,
  ImageContent,
  RichTextContent,
  SheetBlockContent,
} from 'components';

const contentComponents = {
  AlertContent,
  ImageContent,
  RichTextContent,
  SheetBlockContent,
};

export default contentComponents;
